// import * as React from 'react';
// import { configure, shallow } from 'enzyme';
// import Adapter from 'enzyme-adapter-react-16';
// import App from './App';
//
// configure({
//     adapter: new Adapter()
// });
//
describe('<App />', () => {
    // let wrapper: any;
    // beforeEach(() => {
    //     wrapper = shallow(<App/>);
    // });
    // it('should render only once', () => {
    //     expect(wrapper).toHaveLength(1);
    // })
    it('should have atleast one useless test', () => {
        expect(1).toBe(1);
    })
});
